# nodejs-webservice-example
sample app of web service

## setup
`npm install`
`npm install -g mocha`

## Contents
- `index.js` the main entry point
- `hello.js` the hello world example
- `hello.sh` the standalone bash script example with nodejs
